'use client'

import { useMemo, useState } from 'react'
import Link from 'next/link'
import { AlertTriangle, ArrowLeft, BarChart3, Bell, BookOpen, CalendarDays, Check, ChevronRight, Download, FileText, Info, LockKeyhole, MoreHorizontal, Pencil, Plus, Search, Settings, ShieldCheck, UserPlus, Users, X, GraduationCap } from 'lucide-react'
import { AdminContent, AdminShell, PageHeader, Panel, primaryButton, secondaryButton, StatusBadge } from './admin-shell'

type User = { name: string; email: string; role: string; status: string }
const initialUsers: User[] = [
  { name: 'Admin Super', email: 'admin@college.edu.in', role: 'Super Admin', status: 'Active' },
  { name: 'Rohit Verma', email: 'rohit.v@college.edu.in', role: 'Admin', status: 'Active' },
  { name: 'Sneha Reddy', email: 'sneha.r@college.edu.in', role: 'Admin', status: 'Active' },
  { name: 'Vikram Patel', email: 'vikram.p@college.edu.in', role: 'Admin', status: 'Active' },
  { name: 'Ananya Joshi', email: 'ananya.j@college.edu.in', role: 'Viewer', status: 'Active' },
  { name: 'Neha Singh', email: 'neha.s@college.edu.in', role: 'Viewer', status: 'Inactive' },
  { name: 'Arjun Mehta', email: 'arjun.m@college.edu.in', role: 'Admin', status: 'Active' },
  { name: 'Pooja Sharma', email: 'pooja.s@college.edu.in', role: 'Viewer', status: 'Inactive' },
]
const input = 'h-10 rounded-lg border border-border bg-white px-3 text-sm outline-none focus:border-blue-600'
function Modal({ title, children, close }: { title: string; children: React.ReactNode; close: () => void }) { return <div className="fixed inset-0 z-50 flex items-center justify-center bg-navy/40 p-4"><div className="w-full max-w-lg rounded-2xl border border-border bg-white shadow-xl"><div className="flex items-center justify-between border-b border-border px-5 py-4"><h2 className="font-semibold text-navy">{title}</h2><button aria-label="Close" onClick={close} className="rounded-lg p-2 text-slate-400 hover:bg-slate-50"><X className="size-4" /></button></div><div className="p-5">{children}</div></div></div> }
function Select({ label, options, value }: { label: string; options: string[]; value: string }) { return <label className="block text-sm font-medium text-slate-700">{label}<select defaultValue={value} className={`mt-1.5 w-full ${input}`}>{options.map(option => <option key={option}>{option}</option>)}</select></label> }
function Notice({ children }: { children: React.ReactNode }) { return <div className="flex items-start gap-2 rounded-xl border border-amber-100 bg-amber-50 p-4 text-sm text-amber-900"><Info className="mt-0.5 size-4 shrink-0" />{children}</div> }

export function UsersRolesPage() {
  const [users, setUsers] = useState(initialUsers)
  const [query, setQuery] = useState('')
  const [modal, setModal] = useState<User | 'add' | null>(null)
  const [toast, setToast] = useState('')
  const visible = useMemo(() => users.filter(user => `${user.name} ${user.email}`.toLowerCase().includes(query.toLowerCase())), [users, query])
  function save() { if (modal === 'add') setUsers(current => [...current, { name: 'New User', email: 'new.user@college.edu.in', role: 'Viewer', status: 'Active' }]); setModal(null); setToast('User saved successfully.') }
  return <AdminShell><AdminContent><PageHeader title="System Users & Roles" description="Manage authorized system users" actions={<button onClick={() => setModal('add')} className={primaryButton}><Plus className="size-4" /> Add User</button>} /><Panel className="mb-5"><div className="flex flex-col gap-4 p-5 md:flex-row md:items-end"><label className="block flex-1 text-sm font-medium text-slate-700">Search by name or email<div className="relative mt-1.5"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search by name or email" className={`w-full pl-10 ${input}`} /></div></label><Select label="Role" value="All" options={['All', 'Super Admin', 'Admin', 'Viewer']} /><Select label="Status" value="All" options={['All', 'Active', 'Inactive']} /></div></Panel><div className="mb-4 rounded-lg border border-blue-100 bg-blue-50 px-4 py-2 text-sm">Total Users: <strong className="text-navy">{users.length}</strong></div><Panel><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-muted-foreground"><tr><th className="px-5 py-3">User</th><th className="px-5 py-3">Role</th><th className="px-5 py-3">Status</th><th className="px-5 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-border">{visible.map(user => <tr key={user.email}><td className="px-5 py-4"><p className="font-semibold text-navy">{user.name}</p><p className="text-xs text-muted-foreground">{user.email}</p></td><td className="px-5 py-4"><StatusBadge tone="blue">{user.role}</StatusBadge></td><td className="px-5 py-4"><StatusBadge tone={user.status === 'Active' ? 'green' : 'red'}>{user.status}</StatusBadge></td><td className="px-5 py-4 text-right"><button aria-label={`Edit ${user.name}`} onClick={() => setModal(user)} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100"><Pencil className="size-4" /></button><button aria-label={`Toggle ${user.name}`} onClick={() => setUsers(current => current.map(item => item.email === user.email ? { ...item, status: item.status === 'Active' ? 'Inactive' : 'Active' } : item))} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100"><MoreHorizontal className="size-4" /></button></td></tr>)}</tbody></table></div></Panel><div className="mt-6"><Notice><ShieldCheck className="mr-1 inline size-4" />Only authorized Admin users can manage system users and roles.</Notice></div></AdminContent>{modal && <Modal title={modal === 'add' ? 'Add User' : 'Edit User'} close={() => setModal(null)}><div className="grid gap-4"><label className="text-sm font-medium text-slate-700">Full Name<input defaultValue={modal === 'add' ? '' : modal.name} className={`mt-1.5 w-full ${input}`} /></label><label className="text-sm font-medium text-slate-700">Email<input defaultValue={modal === 'add' ? '' : modal.email} className={`mt-1.5 w-full ${input}`} /></label><Select label="Role" value={modal === 'add' ? 'Viewer' : modal.role} options={['Super Admin', 'Admin', 'Viewer']} /><div className="flex justify-end gap-2"><button onClick={() => setModal(null)} className={secondaryButton}>Cancel</button><button onClick={save} className={primaryButton}>Save Changes</button></div></div></Modal>}{toast && <button onClick={() => setToast('')} className="fixed bottom-5 right-5 z-50 rounded-lg bg-navy px-4 py-3 text-sm font-semibold text-white shadow-xl">{toast}</button>}</AdminShell>
}

const auditRows = ['Timetable Published', 'Bulk Student Update', 'Student Created', 'Faculty Updated', 'Timetable Imported', 'User Added']
export function AuditLogsPage() { const [query, setQuery] = useState(''); const rows = auditRows.filter(row => row.toLowerCase().includes(query.toLowerCase())); return <AdminShell><AdminContent><PageHeader title="Audit Logs" description="View All Activities" /><Panel className="mb-5"><div className="flex flex-col gap-4 p-5 md:flex-row md:items-end"><label className="block flex-1 text-sm font-medium text-slate-700">Search by action or user<input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search by action or user" className={`mt-1.5 w-full ${input}`} /></label><Select label="Action" value="All" options={['All', 'Published', 'Created', 'Updated']} /><Select label="User" value="All" options={['All', 'Admin Super', 'Rohit Verma']} /></div></Panel><Panel><div className="divide-y divide-border">{rows.map((row, index) => <div key={row} className="flex items-start gap-4 p-5"><div className="flex size-10 shrink-0 items-center justify-center rounded-full bg-blue-50 text-blue-600"><FileText className="size-4" /></div><div className="flex-1"><p className="font-semibold text-navy">{row}</p><p className="mt-1 text-sm text-slate-600">Administrative activity recorded in the system</p><p className="mt-2 text-xs text-muted-foreground">{20 - index} May 2024 · Admin Super</p></div><ChevronRight className="size-4 text-slate-400" /></div>)}</div></Panel><div className="mt-6"><Notice><LockKeyhole className="mr-1 inline size-4" />All important administrative actions are recorded with timestamp and user details.</Notice></div></AdminContent></AdminShell> }

const reportCards = [['Attendance Report', 'Daily / Monthly attendance report', CalendarDays], ['Student List Report', 'By department, year, section', Users], ['Subject-wise Attendance', 'Attendance by subject / class', BookOpen], ['Timetable Report', 'Class timetable summary', FileText], ['Inactive Students Report', 'Students with low attendance', AlertTriangle]] as const
export function ReportsPage() { const [tab, setTab] = useState('Overview'); const [toast, setToast] = useState(false); return <AdminShell><AdminContent><PageHeader title="Reports & Analytics" description="Insights & Export" actions={<button onClick={() => setToast(true)} className={secondaryButton}><Download className="size-4" /> Export Reports</button>} /><div className="mb-6 flex gap-6 border-b border-border">{['Overview', 'Attendance', 'Academics', 'Users'].map(item => <button key={item} onClick={() => setTab(item)} className={`border-b-2 px-1 pb-3 text-sm font-semibold ${tab === item ? 'border-blue-600 text-blue-600' : 'border-transparent text-muted-foreground'}`}>{item}</button>)}</div>{tab === 'Overview' ? <><Panel title="Overview Summary"><div className="grid gap-4 p-5 sm:grid-cols-2 lg:grid-cols-4">{[['2,856', 'Total Students'], ['156', 'Total Faculty'], ['96', 'Total Classes'], ['82%', 'Avg Attendance']].map(([value, label]) => <div key={label} className="rounded-xl bg-blue-50 p-4 text-blue-700"><p className="text-2xl font-bold">{value}</p><p className="mt-1 text-sm">{label}</p></div>)}</div></Panel><div className="mt-6"><Panel title="Popular Reports"><div className="divide-y divide-border">{reportCards.map(([title, description, Icon]) => <button key={title} className="flex w-full items-center gap-3 p-5 text-left hover:bg-slate-50"><div className="flex size-10 items-center justify-center rounded-lg bg-blue-50 text-blue-600"><Icon className="size-5" /></div><div className="flex-1"><p className="font-semibold text-navy">{title}</p><p className="text-sm text-muted-foreground">{description}</p></div><ChevronRight className="size-4 text-slate-400" /></button>)}</div></Panel></div></> : <Panel title={`${tab} Insights`}><div className="grid gap-4 p-5 sm:grid-cols-2 lg:grid-cols-4">{['82%', '2,498', '358', '124'].map((value, index) => <div key={value} className="rounded-xl border border-border bg-slate-50 p-4"><p className="text-2xl font-bold text-navy">{value}</p><p className="mt-1 text-sm text-muted-foreground">{['Average Attendance', 'Present Students', 'Absent Students', 'Low Attendance'][index]}</p></div>)}</div></Panel>}<div className="mt-6"><Notice>Reports may contain sensitive data. Share only with authorized personnel.</Notice></div>{toast && <button onClick={() => setToast(false)} className="fixed bottom-5 right-5 z-50 rounded-lg bg-navy px-4 py-3 text-sm font-semibold text-white shadow-xl">Report export prepared successfully.</button>}</AdminContent></AdminShell> }

const settingCards = [['Institution Information', 'Manage college details and branding', BookOpen], ['Academic Settings', 'Manage academic year, semesters, and grading', GraduationCap], ['Attendance Settings', 'Configure attendance rules and marking preferences', Check], ['Notification Settings', 'Manage system and class alerts', Bell], ['Security Settings', 'Password policy, session, and access control', LockKeyhole], ['Backup & Restore', 'Manage data backup and restore', FileText], ['Integration Settings', 'Manage third-party integrations', BarChart3], ['System Preferences', 'General app preferences', Settings]] as const
export function SettingsPage() { const [selected, setSelected] = useState<string | null>(null); return <AdminShell><AdminContent><PageHeader title="System Settings" description="General Configuration" /><div className="grid gap-4 lg:grid-cols-2">{settingCards.map(([title, description, Icon]) => <button key={title} onClick={() => setSelected(title)} className="flex items-center gap-4 rounded-xl border border-border bg-white p-5 text-left shadow-card hover:border-blue-200"><div className="flex size-11 items-center justify-center rounded-xl bg-blue-50 text-blue-600"><Icon className="size-5" /></div><div className="flex-1"><p className="font-semibold text-navy">{title}</p><p className="mt-1 text-sm text-muted-foreground">{description}</p></div><ChevronRight className="size-4 text-slate-400" /></button>)}</div><div className="mt-6"><Notice><LockKeyhole className="mr-1 inline size-4" />Only authorized Admin users can change system configuration.</Notice></div>{selected && <Modal title={selected} close={() => setSelected(null)}><p className="text-sm text-slate-600">Configuration options for {selected} will appear here.</p><div className="mt-5 flex justify-end"><button onClick={() => setSelected(null)} className={primaryButton}>Close</button></div></Modal>}</AdminContent></AdminShell> }

export function TimetablePublishEnhanced() { const [confirm, setConfirm] = useState(false); const [published, setPublished] = useState(false); return <AdminShell><AdminContent><PageHeader title="Publish Timetable" description="Review and publish the selected timetable." actions={<Link href="/admin/timetable" className={secondaryButton}><ArrowLeft className="size-4" /> Back</Link>} /><div className="grid gap-5 lg:grid-cols-2"><Panel title="Publish Summary"><div className="grid gap-4 p-5 sm:grid-cols-2"><div><p className="text-xs text-muted-foreground">Timetable</p><p className="font-semibold text-navy">May – Aug 2024</p></div><div><p className="text-xs text-muted-foreground">Classes</p><p className="font-semibold text-navy">96</p></div><div><p className="text-xs text-muted-foreground">Departments</p><p className="font-semibold text-navy">CSE, ECE, ME, CE, IT, AE</p></div><div><p className="text-xs text-muted-foreground">Total Entries</p><p className="font-semibold text-navy">1,248</p></div></div></Panel><Panel title="Publish To"><div className="grid gap-3 p-5"><div className="rounded-xl border border-emerald-200 bg-emerald-50 p-4"><Check className="mr-2 inline size-5 text-emerald-600" /><strong className="text-navy">Faculty</strong><p className="ml-7 text-sm text-muted-foreground">156 Faculty will get access</p></div><div className="rounded-xl border border-emerald-200 bg-emerald-50 p-4"><Check className="mr-2 inline size-5 text-emerald-600" /><strong className="text-navy">Students</strong><p className="ml-7 text-sm text-muted-foreground">2,856 Students will get access</p></div></div></Panel><Panel title="Publish Date & Time"><div className="grid gap-4 p-5 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700">Date<input className={`mt-1.5 w-full ${input}`} value="20 May 2024" readOnly /></label><label className="text-sm font-medium text-slate-700">Time<input className={`mt-1.5 w-full ${input}`} value="10:30 AM" readOnly /></label></div></Panel><Panel title="Ready to Publish"><div className="p-5"><Notice>Once published, timetable will be visible in the app as per class access.</Notice><div className="mt-5 flex items-center justify-between"><span className="text-sm text-muted-foreground">{published ? 'Published' : 'Pending review'}</span><button disabled={published} onClick={() => setConfirm(true)} className={`${primaryButton} disabled:opacity-50`}>{published ? 'Published' : 'Publish Timetable'}</button></div></div></Panel></div></AdminContent>{confirm && <Modal title="Publish Timetable?" close={() => setConfirm(false)}><p className="text-sm text-slate-600">The selected timetable will become visible to faculty and students.</p><div className="mt-5 flex justify-end gap-2"><button onClick={() => setConfirm(false)} className={secondaryButton}>Cancel</button><button onClick={() => { setConfirm(false); setPublished(true) }} className={primaryButton}>Publish Timetable</button></div></Modal>}</AdminShell> }
